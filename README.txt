npm install -D express
npm install -D path

npm start

git branch "branch-name" -> to create a branch
git checkout -b "branch-name" -> to create a branch AND go to it
git checkout "branch-name" -> to go to an existing branch